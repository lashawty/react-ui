import {type Color, type ThemeKey} from '@/components/types.ts';

export const theme: Record<ThemeKey, Color> = {
	primary: '#a3c4af',
	secondary: '#a3bbc4',
	disable: '#a1a1aa',
	error: '#ff0000',
};
