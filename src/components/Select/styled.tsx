import styled from 'styled-components';

export const StyledSelect = styled.select`
	padding: 10px;
	border-radius: 10px;
	border: 1px solid #fff;
	background: none;
	color: #fff;
	min-width: 180px;
    max-height: 500px;	
  
	&:focus-visible {
		outline: none;
	}
`;

